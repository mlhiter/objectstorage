# Packaged defaults for Sealos app-level overrides.
# Chart defaults stay in values.yaml; cluster-derived values are injected by
# deploy/entrypoint.sh; user overrides are loaded from
# /root/.sealos/cloud/values/apps/objectstorage/*-values.yaml.

objectstorageConfig: {}
